{
  "name": "chip-claw",
  "version": "0.1.0",
  "description": "RTL generation, optimization and verification agent based on chipclaw architecture",
  "type": "module",
  "bin": {
    "chip-claw": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node test/mcp-config-smoke.mjs && node test/knowledge-smoke.mjs && node test/bench-smoke.mjs",
    "start": "node dist/cli.js",
    "dev": "tsc && node dist/cli.js",
    "bench": "tsc && node dist/bench/run.js"
  },
  "keywords": [
    "claude-code",
    "coding-agent",
    "cli",
    "ai",
    "llm"
  ],
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "chalk": "^5.4.1",
    "dotenv": "^17.4.2",
    "glob": "^11.0.1",
    "openai": "^6.33.0",
    "undici": "^6.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  }
}
